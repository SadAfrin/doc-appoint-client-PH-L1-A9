import LoginMain from "@/components/auth/LoginMain";

export const metadata = {
  title: "Login | Doctor Appointment",
  description: "Login to your account to manage appointments.",
};

export default function LoginPage() {
  return <LoginMain />;
}